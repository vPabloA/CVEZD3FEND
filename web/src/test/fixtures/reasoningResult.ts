import type { ReasoningResult } from "@/lib/reasoningTypes";

/** Minimal-but-complete ReasoningResult fixture for tests (test-only — never used by product components). */
export function makeReasoningResult(overrides: Partial<ReasoningResult> = {}): ReasoningResult {
  const base: ReasoningResult = {
    input: "cve-2024-0001",
    normalized_input: "CVE-2024-0001",
    status: "ok",
    source_mode: "live",
    baseline_provider: "nvd",
    reasoning_mode: "full",
    human_review: { required: false, reason: "" },
    risk: {
      cvss: { base_score: 9.8, vector: "AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H" },
      epss: { score: 0.94, percentile: 0.99 },
      kev: { listed: true, date_added: "2024-02-01" },
      exploitability: { exploit_available: true },
    },
    route: {
      canonical_chain: ["CVE-2024-0001", "CWE-89", "CAPEC-66", "T1190", "D3-IOPR"],
      primary_nodes: ["CWE-89", "CAPEC-66"],
      secondary_nodes: ["T1190"],
      conditional_nodes: ["D3-NTA"],
      defensive_nodes: ["D3-IOPR", "MIT-M1030"],
      weak_fit_nodes: ["CAPEC-7"],
    },
    edges: [
      {
        id: "edge-1",
        source: "CVE-2024-0001",
        target: "CWE-89",
        type: "exploits_weakness",
        classification: "official_explicit",
        confidence: 0.95,
        evidence: ["NVD CWE mapping"],
        source_refs: ["nvd"],
        source_url: "https://nvd.nist.gov/vuln/detail/CVE-2024-0001",
        note: null,
        deterministic: true,
        inferred: false,
        conditional: false,
        weak_fit: false,
      },
      {
        id: "edge-2",
        source: "CWE-89",
        target: "CAPEC-66",
        type: "enables_pattern",
        classification: "analytical_inferred",
        confidence: 0.62,
        evidence: ["AI-inferred from CWE/CAPEC co-occurrence"],
        source_refs: [],
        source_url: null,
        note: "Proposed by reasoning engine, not yet reviewed.",
        deterministic: false,
        inferred: true,
        conditional: false,
        weak_fit: false,
      },
      {
        id: "edge-3",
        source: "T1190",
        target: "D3-NTA",
        type: "mitigated_by",
        classification: "conditional",
        confidence: 0.5,
        evidence: ["Applies only if network traffic analysis is deployed at the perimeter"],
        source_refs: [],
        source_url: null,
        note: "Conditional on network visibility.",
        deterministic: false,
        inferred: false,
        conditional: true,
        weak_fit: false,
      },
    ],
    provenance: {
      nvd: [
        {
          id: "edge-1",
          source: "CVE-2024-0001",
          target: "CWE-89",
          type: "exploits_weakness",
          classification: "official_explicit",
          confidence: 0.95,
          evidence: ["NVD CWE mapping"],
          source_refs: ["nvd"],
          source_url: "https://nvd.nist.gov/vuln/detail/CVE-2024-0001",
          note: null,
          deterministic: true,
          inferred: false,
          conditional: false,
          weak_fit: false,
        },
      ],
    },
    narrative: {
      summary_es: "Esta CVE permite inyección SQL remota sin autenticación.",
      executive_summary_es: "Vulnerabilidad crítica explotada activamente (KEV).",
      decision_context_es: "Priorizar parcheo inmediato por explotación activa.",
      risk_rationale_es: "CVSS 9.8 y presencia en el catálogo KEV de CISA.",
      tier1_conclusion_es: "Acción de Tier 1: aislar el activo y aplicar el parche.",
    },
    soc_action_pack: {
      validations: ["Confirm affected version is deployed"],
      detections: ["Alert on anomalous SQL error rates"],
      containment: ["Isolate affected host from the network"],
      owners: ["SOC Tier 1"],
      evidence_expected: ["WAF logs", "Database query logs"],
    },
    detection_engineering: {
      hypotheses: ["Attacker injects SQL via the search parameter"],
      log_sources: ["WAF", "Database audit log"],
      rule_ideas: ["Alert on UNION SELECT in HTTP parameters"],
      gaps: ["No database query logging enabled"],
    },
    threat_hunting: {
      hypotheses: ["Look for prior exploitation attempts in WAF logs"],
      queries: ["index=waf \"UNION SELECT\""],
      pivot_points: ["Source IPs with repeated 5xx on the affected endpoint"],
    },
    ctem: {
      priority: "critical",
      remediation_actions: ["Apply vendor patch 1.2.3"],
      validation_steps: ["Re-scan with vulnerability scanner"],
      residual_risk: "Low after patch and WAF rule deployment.",
    },
    exports: {
      markdown: "# CVE-2024-0001 Reasoning Report\n\nCritical SQL injection, actively exploited.",
      tree: "CVE-2024-0001\n└── CWE-89\n    └── CAPEC-66",
      mermaid: "graph TD\n  A[CVE-2024-0001] --> B[CWE-89]",
      navigator_layer: null,
    },
    warnings: [],
    errors: [],
  };
  return { ...base, ...overrides };
}
